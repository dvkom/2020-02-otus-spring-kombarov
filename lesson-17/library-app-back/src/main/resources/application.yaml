spring:
  datasource:
    url: ${DB_URL}
    driverClassName: ${DB_DRIVER}
    username: ${DB_USERNAME}
    password: ${DB_PASSWORD}

    initialization-mode: always
    platform: prod

  jpa:
    generate-ddl: false
    hibernate:
      ddl-auto: none

    show-sql: true